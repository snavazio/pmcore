# PMCore — Python dependencies
# Install with: pip install -r requirements.txt
# Or with uv:   uv pip install -r requirements.txt
#
# PyTorch: install separately with CUDA support for your platform
# See https://pytorch.org/get-started/locally/
# Example (CUDA 12.4): pip install torch --index-url https://download.pytorch.org/whl/cu124
# CPU only:            pip install torch

torch>=2.1.0
transformers>=4.40.0
peft>=0.10.0
accelerate>=0.27.0
datasets>=2.16.0
huggingface-hub>=0.20.0
fastapi>=0.110.0
uvicorn>=0.27.0
json-repair>=0.10.0
